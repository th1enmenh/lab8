<table class="table table-bordered">
    <tr>
        <th>Name</th>
        <th>Age</th>
        <th>Email</th>
        <th>Image</th>
    </tr>
    {{#each students}}
    <tr>
        <th>{{name}}</th>
        <th>{{age}}</th>
        <th>{{email}}</th>
        <th><img src="{{image}}" width="100" height="100"</th>
    </tr>
    {{/each}}
</table>